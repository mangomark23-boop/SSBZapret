fn main() {
    // Манифест специально НЕ встраиваем: Rust добавляет свой манифест
    // по умолчанию, и второй манифест вызывает ошибку SxS
    // («параллельная конфигурация неправильна»).
    // Права администратора запрашиваются в рантайме (см. main.rs).
    tauri_build::build();
}
